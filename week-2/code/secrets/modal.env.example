# Copy to one of:
#   secrets/modal.env     (instructor — gitignored)
#   ../.env               (students — copy from .env.example at class2 root)
#
# Get tokens: https://modal.com/settings → API Tokens → Create new token
# Do not commit files containing real values.

MODAL_TOKEN_ID=ak-xxxxxxxxxxxxxxxxxxxxxxxx
MODAL_TOKEN_SECRET=as-xxxxxxxxxxxxxxxxxxxxxxxx

# Fixed app names — see scripts/modal.sh compare
